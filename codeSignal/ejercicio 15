function solution(picture) {
    
let matriz = [];

   matriz.push('*'.repeat(picture[0].length  + 2));

  for (let i = 0; i < picture.length; i++) {
    let fila = '*' + picture[i] + '*';
    matriz.push(fila);
  }

  matriz.push('*'.repeat(picture[0].length + 2));

  return matriz;
}
